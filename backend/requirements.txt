# fastapi==0.115.0
# uvicorn[standard]==0.30.6
# sqlalchemy==2.0.35
# psycopg2-binary==2.9.9
# pydantic==2.9.2
# pydantic-settings==2.5.2
# python-multipart==0.0.9
# python-dotenv==1.0.1
# numpy==1.26.4
# Pillow==10.4.0
# alembic==1.13.2

# # --- Face recognition stack ---
# # dlib-binary ships a precompiled Windows wheel (no CMake/C++ compiler needed).
# dlib-binary==19.24.1
# # face_recognition's own metadata requires plain "dlib" (not dlib-binary), which
# # would force pip to compile dlib from source and fail on Windows without
# # Visual C++ Build Tools. So face_recognition is installed separately with
# # --no-deps (see install_windows.txt / README) and its real remaining
# # dependencies are pinned explicitly here instead:
# face-recognition-models==0.3.0
# click>=8.0























fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
python-dotenv==1.0.1
numpy==1.26.4
Pillow==10.4.0
alembic==1.13.2

# --- Face recognition stack ---
# dlib-binary ships a precompiled Windows wheel (no CMake/C++ compiler needed).
dlib-binary==19.24.1
# face_recognition's own metadata requires plain "dlib" (not dlib-binary), which
# would force pip to compile dlib from source and fail on Windows without
# Visual C++ Build Tools. So face_recognition is installed separately with
# --no-deps (see install_windows.txt / README) and its real remaining
# dependencies are pinned explicitly here instead:
face-recognition-models==0.3.0
click>=8.0